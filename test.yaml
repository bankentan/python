def list = [111111, 222222]
def str = '33333333;4444444444;555555555'

// 将列表元素连接成一个字符串，使用分号分隔
def joinedList = list.join(';')

// 将连接后的列表字符串添加到原始字符串中
def result = str + ";" + joinedList

println result
